{
  "name": "@x-builder/calibration",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "x-cal-normalize": "./src/bin/normalize.ts",
    "x-cal-predict": "./src/bin/predict.ts",
    "x-cal-fit": "./src/bin/fit.ts",
    "x-cal-validate": "./src/bin/validate.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@x-builder/engine": "workspace:*",
    "@x-builder/shared": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
