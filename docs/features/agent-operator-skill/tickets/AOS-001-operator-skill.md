# AOS-001 Operator Skill

## Goal

Create the `x-builder-operator` Codex skill with practical instructions for running, inspecting, and explaining local x-builder.

## Acceptance Criteria

- Skill frontmatter uses name `x-builder-operator` and describes concrete trigger scenarios.
- Skill includes build/run commands for the overlay runner.
- Skill explains Chrome CDP reconnect mode and dedicated launch mode.
- Skill covers local SQLite boundaries, archive import, active context, feedback-loop linking, and live-capture safety.
- Skill anchors explanations to current repo packages and files.

## RGB/TDD

- Red: feature README names a likely skill artifact, but no versioned skill exists.
- Green: add a valid skill folder under `docs/features/agent-operator-skill/skills/x-builder-operator/`.
- Blue: keep instructions operational and concise; do not duplicate large source docs.
