# Deterministic Engine Assets

Reference screenshots for the deterministic engine UI cards belong here.

Expected files:

- `engagement-prediction-card-reference.png`
- `post-coach-card-reference.png`

These are visual references only. Canonical deterministic logic lives in `engine/src/deterministic/post-analyzer.ts`.
